describe('crNavigationService', function() {

  beforeEach(module('common'));

  it('should ...', inject(function(crNavigationService) {

    //expect(crNavigationService.doSomething()).toEqual('something');

  }));

});
